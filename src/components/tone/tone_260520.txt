--- File: ToneGrid.astro ---
---
/**
 * Tone panel grid.
 */

import "./tone.css";

import TonePanel from "./TonePanel.astro";

import type { TonePanelData } from "./tone";

interface Props {
  panels: TonePanelData[];
}

const { panels } = Astro.props;
---

<div class="tone-grid">
  {panels.map((panel) => <TonePanel panel={panel} />)}
</div>


--- File: TonePanel.astro ---
---
/**
 * Tone panel.
 */

import Heading from "../typography/Heading.astro";
import Surface from "../ui/Surface.astro";

import { cn } from "@/utils/cn";

interface Props {
  tag: string;
  title: string;
  description: string;
  variant?: "cold" | "warm";
}

const { tag, title, description, variant = "cold" } = Astro.props;
---

<Surface as="article" variant={variant} padding="lg">
  <span class="tone-panel__tag label-mono">
    {tag}
  </span>

  <Heading level={3} variant="card">
    {title}
  </Heading>

  <p class="prose-muted">
    {description}
  </p>
</Surface>


--- File: tone.css ---
.tone-panel {
  padding: var(--space-6);

  border: var(--border-soft);
}

.tone-panel--cold {
  background: rgba(15, 14, 10, 0.82);
}

.tone-panel--warm {
  background: rgba(200, 168, 75, 0.05);
}

--- File: tone.ts ---
/**
 * Tone section types.
 */

export interface TonePanelData {
  tag: string;
  title: string;
  description: string;
  variant?: "mi5" | "beatles";
}


