---
/**
 * Vertical stack layout primitive.
 */

import { cn } from "@/utils/cn";

interface Props {
  gap?: "xs" | "sm" | "md" | "lg";
  className?: string;
}

const { gap = "md", className } = Astro.props;
---

<div class={cn("stack", `stack--${gap}, ${className}`)}>
  <slot />
</div>

<style>
  .stack {
    display: flex;
    flex-direction: column;
  }

  .stack--xs > * + * {
    margin-top: var(--space-1);
  }

  .stack--sm > * + * {
    margin-top: var(--space-3);
  }

  .stack--md > * + * {
    margin-top: var(--space-5);
  }

  .stack--lg > * + * {
    margin-top: var(--space-7);
  }
</style>
