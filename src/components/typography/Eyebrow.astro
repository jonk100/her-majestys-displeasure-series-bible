---
/**
 * Reusable eyebrow label.
 */
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
  style?: string;
}

const { as: Tag = "div", class: className, style } = Astro.props;
---

<Tag class:list={["eyebrow", className]} style={style}>
  <slot />
</Tag>
