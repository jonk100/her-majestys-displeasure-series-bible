--- File: tokens.css ---
:root {
  /* Colors */
  --color-cream: #f2ead8;
  --color-ink: #0f0e0a;
  --color-paper: #ede5cf;

  --color-amber: #c8a84b;
  --color-amber-dark: #8a6e2a;

  --color-red: #8b1a1a;

  --color-muted: #6b6456;

  /* Typography */
  --font-serif: 'Playfair Display', serif;
  --font-sans: 'IBM Plex Sans', sans-serif;
  --font-mono: 'IBM Plex Mono', monospace;

  /* Layout */
  --container-width: 1100px;
  --section-padding: clamp(4rem, 8vw, 7rem);

  /* Spacing */
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 1rem;
  --space-4: 1.5rem;
  --space-5: 2rem;
  --space-6: 3rem;
  --space-7: 4rem;
  --space-8: 6rem;

  /* Borders */
  --border-soft: 1px solid rgba(200, 168, 75, 0.12);
  --border-faint: 1px solid rgba(200, 168, 75, 0.08);

  /* Effects */
  --transition-base: 180ms ease;
}

--- File: global.css ---
@import './tokens.css';
@import './reset.css';
@import './typography.css';
@import './utilities.css';

--- File: utilities.css ---
.container {
  width: min(var(--container-width), calc(100% - 3rem));
  margin-inline: auto;
}

.section-padding {
  padding-block: var(--section-padding);
}

.stack-sm > * + * {
  margin-top: var(--space-3);
}

.stack-md > * + * {
  margin-top: var(--space-5);
}

.stack-lg > * + * {
  margin-top: var(--space-7);
}

.rule {
  width: 48px;
  height: 1px;

  margin-bottom: var(--space-5);

  background: var(--color-amber);
  opacity: 0.5;
}

--- File: typography.css ---
body {
  line-height: 1.5;
}

.eyebrow {
  margin-bottom: var(--space-3);

  font-family: var(--font-mono);
  font-size: 0.65rem;
  letter-spacing: 0.25rem;
  text-transform: uppercase;

  color: var(--color-amber);
  opacity: 0.85;
}

.section-title {
  margin-bottom: var(--space-5);

  font-family: var(--font-serif);
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 700;
  line-height: 1.1;

  color: var(--color-cream);
}

.section-title em {
  font-style: italic;
  color: var(--color-amber);
}

.prose {
  max-width: 760px;

  font-size: clamp(0.95rem, 1.4vw, 1.08rem);
  line-height: 1.85;

  color: rgba(242, 234, 216, 0.72);
}

.prose + .prose {
  margin-top: var(--space-4);
}

.prose strong {
  font-weight: 500;
  color: var(--color-cream);
}

.mono-label {
  font-family: var(--font-mono);
  text-transform: uppercase;
  letter-spacing: 0.15rem;
}

.label-mono {
  font-family: var(--font-mono);
  font-size: 0.58rem;
  letter-spacing: 0.2rem;
  text-transform: uppercase;

  color: var(--color-amber);
}

.heading-card {
  font-family: var(--font-serif);
  font-size: 1.4rem;
  font-weight: 700;

  color: var(--color-cream);
}

.prose-muted {
  line-height: 1.8;

  color: rgba(242, 234, 216, 0.65);
}

--- File: reset.css ---
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
}

body {
  min-height: 100vh;
  overflow-x: hidden;

  background: var(--color-ink);
  color: var(--color-cream);

  font-family: var(--font-sans);
  font-weight: 300;
}

img,
picture,
video,
canvas,
svg {
  display: block;
  max-width: 100%;
}

button,
input,
textarea,
select {
  font: inherit;
}

a {
  color: inherit;
  text-decoration: none;
}

